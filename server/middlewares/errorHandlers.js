
module.exports = (err, req, res, next) => {
    if (err) {
        switch (err) {
            case value:

                break;

            default:
                res.status(500).json({
                    err
                })
                break;
        }
    }
}